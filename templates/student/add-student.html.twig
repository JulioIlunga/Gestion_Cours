{% extends 'template.html.twig' %}


{% block title %}<title>Student form</title>{% endblock %}


{% block body %}
    {{ form(form) }}
    {# <div class="row">
    <div class="col">{{ form_row('firstname') }}</div>
    <div class="col">{{ form_row('name') }}</div>
    </div> #}
    
    
{% endblock %}

